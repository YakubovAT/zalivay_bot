# Сценарии бота Zalivai

> **Правило:** КАЖДЫЙ экран содержит кнопки навигации. Ни одного экрана без кнопок.

---

## Универсальный сценарий

### Шаг 1. /start — Приветствие

**Триггер:** пользователь нажал `/start`

**Проверка:** `is_registered = TRUE/FALSE`

```
[Баннер + caption]
"Система массовой автоматизированной генерации профессионального
фото и видео контента для товаров с последующим размещением в социальных сетях.

Возможно создавать фото и видео в различных форматах по заранее спроектированным промптам для ваших товаров."

[Inline: Начать →]
```

**Переход:** Нажал «Начать →» → Шаг 2

---

### Шаг 2. Профиль пользователя (статистика)

```
[Баннер + caption]
"👤 {FirstName} (ID: {user_id})

📊 Ваша статистика:
📦 Товаров: {articles_count}
📸 Эталонов: {refs_count}
🖼 Сгенерировано фото: {photos_count}
🎬 Сгенерировано видео: {videos_count}
💰 Баланс: {balance}₽"

[Inline:
  ➕ Новый артикул
  📸 Генерировать фото
  🎥 Генерировать видео
  📂 Мои эталоны
  💰 Пополнить баланс
]
```

**Переходы:**
- «➕ Новый артикул» → Шаг 3
- «📸 Генерировать фото» → Flow генерации фото
- «🎥 Генерировать видео» → Flow генерации видео
- «📂 Мои эталоны» → Flow просмотра эталонов
- «💰 Пополнить баланс» → Flow оплаты

---

### Шаг 3. Выбор маркетплейса

```
[Баннер + caption]
"На этом шаге выбирайте маркетплейс на котором продается Ваш товар, после мы с Вами создадим фото и видео контент для последующего размещения в социальных сетях. Вам нужно будет ввести артикул товара и создам эталон Вашего товара для генерации фото и видео контента"

[Inline:
  ✅ WB
  🔒 Ozon
  🔒 YM
]
[Inline: ← Назад]
```

**Переходы:**
- «WB» → Шаг 4
- «← Назад» → Шаг 2

---

### Шаг 4. Ввод артикула

```
[Баннер + caption]
"Шаг 4: Ввод артикула

В строку сообщений введите артикул.

Мы загрузим фото из карточки. Выберите
3 лучших — где ваш товар виден наиболее
чётко и детально. Это станет основой
для генерации фото и видео контента."

[Inline: ← Назад | 🏠 Меню]
```

**Переход:** Пользователь ввёл текст (цифры или ссылку, нужно вычленить из ссылки артикул, он как правило 6-9 цифр подряд).

---

### Окно скачивания (временное)

```
[Баннер + caption]
"⏳ Ищу товар...1"
(через 1 сек)
"⏳ Ищу товар...2"
(через 1 сек)
"⏳ Ищу товар...3"
...
(максимум до 15)
→ Это ВРЕМЕННОЕ сообщение удаляется сразу после ответа от wb_parser
```

**Особенность:**
- Это **отдельное сообщение**, появляется поверх экрана Шага 4
- Экран Шага 4 **остаётся видимым** под ним
- Когда парсинг завершён → Окно скачивания **удаляется** (как будто его не было)
- Экран Шага 4 **перерисовывается** в экран Шага 5 (редактируется `edit_message_media`)

**Действия:**
- Вызов `wb_parser.get_product_info(articul)`
- Состав очищается от цифр и процентов: `"полиэстер 90%; вискоза 10%"` → `"полиэстер; вискоза"`
- Если товар найден → Шаг 5 (перерисовка Шага 4)
- Если товар НЕ найден → Шаг 4а (АЛЕРТ)

---

### Шаг 4а. Товар НЕ найден — АЛЕРТ

```
🔴 [БЕЗ баннера, обычный текст]
"❌ Артикул {articul} не найден. Проверьте и попробуйте снова."

→ Удаляется автоматически через 5 секунд
```

**Что видит пользователь после удаления алерта:**
```
[Баннер — тот же что и в Шаге 4]
"Шаг 4: Ввод артикула..."

[Inline: ← Назад | 🏠 Меню]
```

**Переход:** Пользователь вводит новый артикул → Окно скачивания

---

### Шаг 5. Показ карточки товара

> **Важно:** Этот экран получается **редактированием** экрана Шага 4, а не созданием нового. Окно скачивания к этому моменту уже удалено.

```
[Баннер + ПЕРВОЕ фото товара из WB + caption]
"Шаг 5: Найден товар

📦 {name}
🏷 Бренд: {brand}
🎨 Цвет: {color}
🧵 Состав: {material}

Это тот товар?"

[Inline: ✅ Да, это он | ❌ Нет, другой]
[Inline: ← Назад | 🏠 Меню]
```

**Данные из wb_parser:**
- `name` — название товара
- `brand` — бренд
- `color` — первый цвет из списка
- `material` — состав (без цифр и процентов)
- `images` — список URL всех фото товара (до 30 штук)

**Переходы:**
- «❌ Нет, другой» → Шаг 4 (повторный ввод артикула)
- «✅ Да, это он» → Шаг 6
- «← Назад» → Шаг 3
- «🏠 Меню» → Шаг 2

---

### Шаг 6. Выбор фото из карточки

> **Папка:** `media/{user_id}/WB/{articul}/` — создаётся при подтверждении товара. Скачиваются все фото (до 30 штук).
> **Важно:** Это **один экран**, который плавно меняется. Сначала появляется "Загрузка фото..." (5 сек с анимацией), затем он же превращается в экран выбора.

```
[Баннер + фото товара + caption]
"Шаг 6 из N: Выбор фото — 1 из 12

Выберите фото №1 — где товар виден лучше всего."

[Inline: ⚪ 1  ⚪ 2  ⚪ 3]
[Inline: ← Пред.  1/12  След. →]
[Inline: ← Назад | 🏠 Меню]
```

**Логика выбора:**

Нажал ⚪ 1 -> Выбралось, фото скрылось -> Переход к следующему свободному.
Нажал ⚪ 2 -> Выбралось, фото скрылось -> Переход к следующему свободному.
Нажал ⚪ 3 -> Выбралось, фото скрылось -> Переход к следующему свободному (или появление кнопки "Утвердить").
Нажал на уже выбранное (🔘 1) -> Выбор отменился, фото появилось в списке.
**Переходы:**
- «← Пред.» / «След. →» — листают фото (редактирование текущего экрана)
- `⚪ 1/2/3` — выбор фото в слот (кнопка становится `🔘 1/2/3`)
- «✅ Утвердить выбор» → Шаг 7 (показ коллажа)
- «← Назад» → Шаг 3
- «🏠 Меню» → Шаг 2

---

### Шаг 7. Создание эталона (Коллаж)

> На этом шаге бот **склеивает** 3 выбранных фото в одно изображение (горизонтальный коллаж).

```
[Баннер + КОЛЛАЖ из 3-х фото + caption]
"Шаг 7 из N: Создание эталона

Вы выбрали 3 фото для артикула 12345678.

Убедитесь, что на этих фото товар виден лучше всего —
по ним будет создан эталон для генерации контента."

[Inline: ✅ Создать эталон]
[Inline: ← Назад | 🏠 Меню]
```

**Действия:**
- 3 фото склеиваются в один файл `media/{user_id}/temp/{articul}_collage.png`.
- Выбранные фото и артикул сохранены в контексте для следующего шага.

**Переходы:**
- «✅ Создать эталон» → Шаг 8 (проверка баланса → генерация T2I)
- «← Назад» → Шаг 6 (к выбору фото)
- «🏠 Меню» → Шаг 2

---

### Шаг 8. Проверка: артикул уже есть в базе

**Проверка:** `SELECT COUNT(*) FROM article_references WHERE user_id = X AND articul = Y AND is_active = TRUE`

#### 8а. Артикул НОВЫЙ → Шаг 9

#### 8б. Артикул УЖЕ СУЩЕСТВУЕТ → Шаг 8б-1

---

### Шаг 8б-1. Артикул уже существует

```
[Баннер + caption]
"⚠️ У вас уже есть товар:

📦 {name} ({articul})
📸 Эталонов: {refs_count}

Хотите создать ещё один эталон?"

[Inline:
  ✅ Да, создать ещё один
  📂 Посмотреть существующие
]
[Inline: ❌ Нет | ← Назад (к карточке) | 🏠 Меню]
```

**Переходы:**
- «✅ Да» → Шаг 9
- «📂 Посмотреть» → Шаг 15 (просмотр эталонов)
- «❌ Нет» → Шаг 4
- «← Назад» → Шаг 5
- «🏠 Меню» → Шаг 2

---

### Шаг 9. Предложение создать эталон

```
[Баннер + caption]
"Шаг 9: Создание эталона

Создаю эталон — чистое фото товара на прозрачном
фоне без лишних предметов.

Этот эталон будет использоваться для генерации
фото и видео контента.

💰 Стоимость: {price}₽
💳 Ваш баланс: {balance}₽

Создать эталон?"

[Inline: ✅ Создать эталон]
[Inline: ❌ Отмена | ← Назад | 🏠 Меню]
```

**Действия:**
- Читаем прайслист: `price = get_price('create_reference')`
- Читаем баланс пользователя: `balance = get_user_balance(user_id)`
- Если `balance >= price` → ждём подтверждения
- Если `balance < price` → Шаг 9а (недостаточно средств)

**Переходы:**
- «❌ Отмена» → Шаг 4
- «← Назад» → Шаг 5 (или Шаг 8б-1 если артикул существующий)
- «🏠 Меню» → Шаг 2
- «✅ Создать» → Шаг 10

---

### Шаг 9а. Недостаточно средств — АЛЕРТ

```
[Баннер + caption]
"❌ Недостаточно средств.

💰 Стоимость создания эталона: {REFERENCE_COST}₽
💳 Ваш баланс: {balance}₽

Пополните баланс и попробуйте снова."

[Inline: ❌ Закрыть]
```

**Особенность:**
- Сообщение **НЕ удаляется автоматически** — пользователь сам закрывает кнопкой «❌ Закрыть»
- После закрытия алерта пользователь видит экран Шага 9 (кнопка «Создать эталон» остаётся активной)
- После пополнения баланса можно снова нажать «Создать эталон»

**Переходы:**
- «❌ Закрыть» → алерт удаляется, остаётся Шаг 9

---

### Шаг 10. Создание эталона (T2T → I2I)

**Действия:**
1. Проверка баланса: `balance = get_user_stats(user_id)`
2. Если `balance < REFERENCE_COST` → показываем ошибку и выходим
3. T2T API → генерация промпта + категории (`reference_t2t.py`)
4. Коллаж из 3 фото → загрузка в Telegram → получение URL
5. I2I API → генерация эталона (`reference_i2i.py`)
6. Скачивание результата → списание баланса → сохранение в БД
7. Показ финального результата пользователю

**Экран в процессе (до T2T):**
```
[Баннер + caption]
"⏳ Создаю эталон для артикула {articul}...

Zaliv.AI — сервис массовой автоматизированной генерации
профессионального фото и видео контента для товаров
с последующим размещением в социальных сетях.

Это займёт 1-3 минуты..."
```

**Экран после T2T (перед I2I):**
```
[Баннер + caption]
"⏳ Генерирую фото эталона...
Тип товара: {category}

Созданный эталон позволит вам массово генерировать
фото и видео для любых площадок: Telegram, VK,
Instagram, YouTube и других социальных сетей.

Осталось немного..."
```

**Экран ошибки (недостаточно средств):**
```
[Баннер + caption]
"❌ Недостаточно средств.

💰 Стоимость создания эталона: {REFERENCE_COST}₽
💳 Ваш баланс: {balance}₽

Пополните баланс и попробуйте снова."
```

**Экран результата:**
```
[Фото эталона (отправлено отдельным фото) + caption]
"Шаг 11 из N: Эталон готов!

📦 Артикул: {articul}
📸 Это ваш {ref_number}-й эталон для этого товара
🏷 Тип товара: {category}

💰 Списано: {REFERENCE_COST}₽
💳 Ваш баланс: {new_balance}₽

Эталон может немного отличаться от оригинала.
Если отличия значительные — перегенерируйте эталон,
заменив фотографии на шаге выбора фото.

Теперь вы можете генерировать фото и видео!"

[Inline:
  📸 Генерировать фото  |  🎥 Генерировать видео
]
[Inline:
  📂 Мои эталоны  |  🏠 Меню
]
```

**Переходы:**
- «📸 Генерировать фото» → Flow генерации фото
- «🎥 Генерировать видео» → Flow генерации видео
- «📂 Мои эталоны» → Flow просмотра эталонов
- «🏠 Меню» → Шаг 2

---

### Шаг 11. Утверждение эталона

```
[Баннер + фото эталона + caption]
"Шаг 11: Утверждение эталона

🎉 Эталон готов!

📦 Артикул: {articul}
📸 Это ваш {ref_number}-й эталон для этого товара

Проверьте качество:"

[Inline: ✅ Подтвердить | 🔄 Перегенерировать (-{price}₽)]
[Inline: ← Назад | 🏠 Меню]
```

**Переходы:**

#### 11а. 🔄 Перегенерировать
- Проверяем баланс
- Если `balance >= price` → списываем → Шаг 10 (новая генерация)
- Если `balance < price` → Шаг 9а (недостаточно средств)
- Новый эталон получает `ref_number = N + 1`

#### 11б. ✅ Подтвердить → Шаг 12 (сохранение)

- «← Назад» → возврат к Шагу 9
- «🏠 Меню» → Шаг 2

---

### Шаг 12. Сохранение эталона

**Действия:**
```
article_references:
  - user_id
  - articul
  - reference_number = COUNT(active refs for this articul) + 1
  - file_id (telegram file id)
  - file_path (локальный путь)
  - reference_image_url (публичный URL для I2I API)
  - category (из T2T: верх/низ/обувь/головной убор)
  - reference_prompt (английский промпт из T2T)
  - is_active = TRUE
```

---

### Шаг 13. Финал — эталон сохранён

```
[Баннер + caption]
"✅ Эталон сохранён!

📦 Артикул: {articul}
📸 Эталон #{ref_number} из {total_refs}

Что дальше?"

[Inline:
  📸 Генерировать фото
  🎥 Генерировать видео
  ➕ Новый артикул
]
[Inline: 📂 Мои эталоны | ← Назад | 🏠 Меню]
```

**Переходы:**
- «📸 Генерировать фото» → Flow генерации фото
- «🎥 Генерировать видео» → Flow генерации видео
- «➕ Новый артикул» → Шаг 3
- «📂 Мои эталоны» → Шаг 15
- «← Назад» → Шаг 10
- «🏠 Меню» → Шаг 2

---

## Flow: Просмотр эталонов

### Шаг 15. Список товаров пользователя

```
[Баннер + caption]
"📂 Мои эталоны

Ниже ваши артикулы с эталонами.
Нажмите на артикул — откроется меню работы с эталонами."

[Inline:
  📦 12345678 (3)  |  📦 87654321 (1)
  📦 11223344 (2)  |  📦 99887766 (1)
]
[Inline: 🏠 Меню]
```

> **Формат:** Каждая кнопка — `📦 {articul} ({ref_count})` — артикул + количество эталонов
> Кнопки расположены в 2 столбца.

**Переходы:**
- Нажал на артикул → Шаг 16
- «🏠 Меню» → Шаг 2

**Если эталонов нет:**
```
[Баннер + caption]
"📂 Мои эталоны

У вас пока нет товаров с эталонами.

Создайте первый эталон, чтобы генерировать
фото и видео для ваших товаров."

[Inline: ➕ Добавить товар]
[Inline: 🏠 Меню]
```

---

### Шаг 16. Просмотр эталона

```
[Баннер + фото эталона + caption]
"📸 Эталон #{ref_number} из {total}
� Артикул: {article}
🏷 Тип товара: {category}"

[Inline: ← Пред.  1/{total}  След. →]    (если >1 эталона)
[Inline: 📸 Генерировать фото  |  🎥 Генерировать видео]
[Inline: 📂 Мои эталоны  |  🏠 Меню]
```

**Навигация:**
- «След. →» → следующий эталон (редактируем сообщение)
- «← Пред.» → предыдущий эталон
- «� Генерировать фото» → Flow генерации фото
- «🎥 Генерировать видео» → Flow генерации видео
- «📂 Мои эталоны» → Шаг 15
- «🏠 Меню» → Шаг 2

---

### Шаг 17. Подтверждение удаления эталона

```
[Баннер + caption]
"⚠️ Удалить эталон #{ref_number}?

Это действие нельзя отменить.
Деньги НЕ возвращаются."

[Inline: ⚠️ Да, удалить | ❌ Отмена]
[Inline: ← Назад (к эталону) | 🏠 Меню]
```

**Переходы:**
- «❌ Отмена» → возврат к Шагу 16
- «← Назад» → Шаг 16
- «🏠 Меню» → Шаг 2
- «⚠️ Да, удалить» → soft delete (`is_active = FALSE`) → Шаг 17а

---

### Шаг 17а. Эталон удалён

```
[Баннер + caption]
"🗑 Эталон #{ref_number} удалён.

Осталось эталонов: {remaining}"

[Inline: 📂 К эталонам]
[Inline: ← Назад | 🏠 Меню]
```

**Переходы:**
- «📂 К эталонам» → Шаг 16
- «← Назад» → Шаг 15
- «🏠 Меню» → Шаг 2

---

## Flow: Генерация фото

> **Работаем только с эталонами.** Артикул вводится один раз — при создании эталона.
> Дальше пользователь выбирает из готовых эталонов.

### Путь входа

| Откуда | Что происходит |
|--------|---------------|
| **Карточка эталона (Шаг 16)** → нажал «� Генерировать фото» | Артикул + эталон уже известны → сразу Шаг P1 |
| **Главное меню** → нажал «📸 Генерировать фото» | Переход на Шаг 15 (Мои эталоны) → выбор артикула → выбор эталона → Шаг P1 |
| **Главное меню** → нажал «📂 Мои эталоны» | Шаг 15 → выбор артикула → Шаг 16 → «📸 Генерировать фото» → Шаг P1 |

---

### Шаг P1. Сколько фото?

```
[Баннер + caption]
"📸 Шаг P1: Сколько фото?

Сколько фото сгенерировать на основе этого эталона?

Вы можете сгенерировать одно или множество изображений.
Каждое фото будет уникальным — разная локация, освещение, ракурс.

📦 Артикул: {article}
📸 Эталон: #{ref_number}
🏷 Тип товара: {category}

💰 Стоимость: {PHOTO_COST}₽ за фото

Введите число:"

[Inline: 1  |  5  |  10]
[Inline: ← Назад (к эталону) | 🏠 Меню]
```

**Переходы:**
- Нажал «1», «5», «10» или ввёл число (1-20) → Шаг P2

---

### Шаг P2. Пожелания

```
[Баннер + caption]
"📸 Шаг P2: Пожелания

📦 Артикул: {article}
📸 Эталон: #{ref_number}

Будет сгенерировано: {N} фото
💰 Стоимость: {N × PHOTO_COST}₽

Есть пожелания к генерации?

Например: «хочу фото на фоне моря», «сделай в студии»."

[Inline: Нет пожеланий]
[Inline: ← Назад (к выбору числа) | 🏠 Меню]
```

**Переходы:**
- Ввёл пожелания текстом → Шаг P3
- Нажал «Нет пожеланий» → Шаг P3 (без пожеланий)
- «← Назад» → Шаг P1

---

### Шаг P3. Проверка баланса и подтверждение

```
[Баннер + caption]
"📸 Шаг P3: Подтверждение

Готов генерировать {N} фото на основе изображения представленного выше.

📦 Артикул: {article}
{📝 Пожелания: "{user_wish}"  (если есть)}

💰 Стоимость: {N × PHOTO_COST}₽
💳 Ваш баланс: {balance}₽

Если всё устраивает, нажмите ✅ Сгенерировать и процесс запустится."

[Inline: ✅ Сгенерировать]
[Inline: ← Назад (к пожеланиям) | 🏠 Меню]
```

**Проверка:** Если `balance < N × PHOTO_COST` → алерт "Недостаточно средств" с кнопкой «❌ Закрыть» (НЕ удаляется автоматически)

**Переход:** «✅ Сгенерировать» → Шаг P4

---

### Шаг P4. Генерация (I2I)

**Логика генерации N уникальных промптов:**
- Категория товара из эталона (верх/низ/обувь/головной убор)
- `prompt_generator_cloth.py` генерирует N уникальных lifestyle-промптов
- Каждый промпт содержит: локацию, аутфит, освещение, ракурс
- Если есть пожелания — добавляются к каждому промпту
- **0 вызовов T2T** — всё генерируется локально

**Рандомизация:**
- **Локация:** улица города / кафе / студия / парк / пляж / лофт
- **Освещение:** мягкое утреннее / золотой час / студийное / вечернее
- **Ракурс:** в полный рост / по пояс / крупный план

**Процесс:**
```
1. Проверка баланса: balance >= N × PHOTO_COST
2. Списывается N × PHOTO_COST
3. Генерация N уникальных промптов
4. N вызовов I2I API (каждый со своим промптом)
5. Скачивание результатов → сохранение в media/{user_id}/generated/{article}/
6. Показ результата
```

**Экран процесса:**
```
[Баннер + caption]
"📸 Шаг P4: Генерация

⏳ Генерирую {N} фото для артикула {article}...

Это может занять несколько минут.
Я пришлю результат когда будет готово."

[Inline: 🏠 Меню]
```

---

### Шаг P5. Результат

```
[Первое фото с caption + кнопки]
"📸 Шаг P5: Результат — {N} фото для артикула {article}

📦 Эталон: #{ref_number}
💰 Списано: {N × PHOTO_COST}₽
💳 Остаток: {new_balance}₽"

[Inline:
  📸 Генерировать ещё  |  📂 Мои эталоны
]
[Inline: 🏠 Меню]
[Inline: ✕ Закрыть]
```

- Если фото > 1 — первое фото с caption и кнопками, остальные идут альбомом (media group)
- До 10 фото в одном альбоме
- Кнопка **«✕ Закрыть»** удаляет сообщение с результатом

**Сохранение:** `media/{user_id}/generated/{article}/photo_{article}_{idx}.png`

**Переходы:**
- «📸 Генерировать ещё» → Шаг P1
- «📂 Мои эталоны» → Шаг 15
- «🏠 Меню» → Шаг 2
- «✕ Закрыть» → сообщение удаляется

---

## Справочник

### Типы сообщений

| Тип | Баннер | Авто-удаление | Описание |
|-----|--------|---------------|----------|
| **Экран** | ✅ Да | ❌ Нет | Основной экран с информацией |
| **Промежуточный** | ✅ Да | ✅ Да (сразу) | «⏳ Загружаю...», «⏳ Генерирую...» |
| **Алерт** | ❌ Нет | ✅ Да (5 сек) | Ошибки, предупреждения |

### Сущности БД

| Таблица | Описание |
|---------|----------|
| `users` | Пользователь (balance, is_registered) |
| `articles` | Распарсенные товары (name, color, material, wb_images) |
| `article_references` | Эталоны (N на артикул, ref_number, is_active, product_name, product_color, product_material) |
| `generation_tasks` | Очередь задач (photo/video, status, result_url) |
| `marketplace_cache` | Кэш маркетплейса по артикулу |
| `user_actions` | Лог действий пользователя |

### Прайслист

| Действие | Цена |
|----------|------|
| Создание эталона (REFERENCE_COST) | 5₽ |
| Генерация фото (PHOTO_COST) | 5₽ за фото |
| Генерация видео (VIDEO_COST) | 20₽ |

### Категории товаров

- `верх` — рубашка, футболка, блузка, свитер, куртка
- `низ` — юбка, брюки, шорты, джинсы
- `обувь` — кроссовки, туфли, сапоги
- `головной убор` — шапка, кепка, шляпа
