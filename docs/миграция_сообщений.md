# Миграция сообщений в БД

> Документ описывает процесс переноса UI-сообщений бота и AI-промптов из хардкода в БД,
> механику работы кэш-слоя и текущий статус миграции по каждому блоку.

---

## Зачем

Чтобы тексты, шаблоны и списки редактировались через admin-панель без перезапуска бота.
После сохранения изменений в БД бот подхватывает их автоматически в течение TTL кэша (10 сек в dev, 30–60 в prod).

---

## Механика

### Хранилище

Два типа данных в одной таблице `prompt_templates`:

| Префикс ключа | Тип | Пример |
|---|---|---|
| `photo_*`, `video_*` | AI-промпты для создания | `photo_top`, `video_shoes` |
| `msg_*` | UI-сообщения бота пользователю | `msg_welcome` |

Списки элементов (локации, одежда, цвета) — в таблице `prompt_list_items`.

### Доступ из кода

Все обращения — только через `services/prompt_store.py`:

```python
from services.prompt_store import get_template

text = await get_template("msg_welcome", fallback=_FALLBACK_TEXT)
```

**Приоритет:** БД → `fallback`-аргумент → `_FALLBACK_TEMPLATES` в `prompt_store.py` → пустая строка.

### Публичный API `prompt_store`

| Функция | Назначение |
|---|---|
| `get_template(key, fallback)` | Шаблон или UI-сообщение по ключу |
| `get_list(list_key)` | Список строк (локации, одежда и т.д.) |
| `get_pairs(list_key)` | Список пар `(value, value2)` — локация+движение, одежда+цвет |
| `get_banner(key)` | Путь к баннеру по ключу |
| `invalidate()` | Сброс кэша — вызывается из admin-панели после сохранения |

### Кэш

- In-memory словарь с TTL
- При истечении TTL — запрос к БД
- При недоступности БД — тихий fallback, бот не падает

### Seed данных

Каждое новое сообщение добавляется в `database/schema.sql` блоком:

```sql
DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM prompt_templates WHERE key = 'msg_xxx') THEN
    INSERT INTO prompt_templates (key, template, description, sort_order) VALUES
    ('msg_xxx', 'текст...', 'Шаг N — описание. Переменные: ...', N);
  END IF;
END $$;
```

Проверка по конкретному ключу (не по пустой таблице) — безопасно при повторных запусках `init_db()`.

Значения `sort_order` для `msg_*`: основной флоу 10–100 (шаг 10), эталоны 150–168, фото P1–P4: 200–230, видео V1–V4: 300–330, результаты 400–450, системные 600–610. Промежутки по 10 — чтобы вставить новый шаг между существующими.

### Что можно редактировать через admin

- Текст шаблона вокруг `{placeholder}`
- Порядок отображения сообщений (`sort_order`) — поле `#N` в заголовке каждой карточки раздела «Сообщения»
- Элементы списков (`prompt_list_items`): добавить, отредактировать, отключить (`is_active = FALSE`)

### Что нельзя трогать

- Имена `{placeholder}` — контракт с кодом (иначе бот сломается при подстановке)
- Поле `key` в шаблонах и `list_key` в списках — привязаны к коду

---

## Статус миграции

### AI-промпты — ✅ выполнено

**Файлы:** `services/prompt_generator_cloth.py`, `services/prompt_generator_video.py`

| Ключ | Категория | Переменные |
|------|-----------|------------|
| `photo_top` | Фото — верх | `{description}`, `{item_color}`, `{bottom_item}`, `{location}` |
| `photo_bottom` | Фото — низ | `{description}`, `{item_color}`, `{top_item}`, `{location}` |
| `photo_shoes` | Фото — обувь | `{description}`, `{neutral_outfit}`, `{location}` |
| `photo_hat` | Фото — головной убор | `{description}`, `{neutral_outfit}`, `{location}` |
| `photo_komplekt` | Фото — комплект | `{description}`, `{neutral_outfit}`, `{location}` |
| `video_top` | Видео — верх | `{description}`, `{item_color}`, `{item}`, `{location}`, `{motion}` |
| `video_bottom` | Видео — низ | `{description}`, `{item_color}`, `{item}`, `{location}`, `{motion}` |
| `video_shoes` | Видео — обувь | `{description}`, `{outfit}`, `{location}`, `{motion}` |
| `video_hat` | Видео — головной убор | `{description}`, `{outfit}`, `{location}`, `{motion}` |
| `video_komplekt` | Видео — комплект | `{description}`, `{outfit}`, `{location}`, `{motion}` |

**Списки элементов:**

| list_key | Назначение |
|---|---|
| `photo_locations` | Локации для фото (12 вариантов) |
| `photo_bottom_items` | Элементы низа для категории «верх» |
| `photo_top_items` | Элементы верха для категории «низ» |
| `photo_neutral_outfits` | Нейтральные аутфиты для обуви, шапок, комплектов |
| `photo_colors` | Цвета дополняющей одежды |
| `video_locations` | Локации + движение модели (пары value / value2) |
| `video_bottom_items` | Низ для категории «верх» (пары item / color) |
| `video_top_items` | Верх для категории «низ» (пары item / color) |
| `video_neutral_outfits` | Нейтральные аутфиты для видео |

---

### UI-сообщения — ✅ выполнено

| Ключ | Файл-источник | Экран |
|------|--------------|-------|
| `msg_welcome` | `handlers/flows/onboarding.py` | Шаг 1 — /start, приветствие |
| `msg_profile` | `handlers/flows/messages/common.py` | Шаг 2 — профиль пользователя |
| `msg_marketplace_select` | `handlers/flows/new_article.py` | Шаг 3 — выбор маркетплейса |
| `msg_article_input` | `handlers/flows/new_article.py` | Шаг 4 — ввод артикула |
| `msg_product_found` | `handlers/flows/new_article.py` | Шаг 5 — найден товар / подтверждение |
| `msg_photo_select` | `handlers/flows/photo_selection.py` | Шаг 6 — выбор фото |
| `msg_reference_create_confirm` | `handlers/flows/photo_selection.py` | Шаг 7 — создание эталона (подтверждение) |
| `msg_reference_creating` | `handlers/flows/create_reference.py` | Шаг 8 — создаю эталон для артикула |
| `msg_reference_generating_photo` | `handlers/flows/create_reference.py` | Шаг 10 — создаю фото эталона |
| `msg_reference_ready` | `handlers/flows/create_reference.py` | Шаг 11 — эталон готов |
| `msg_my_refs_empty` | `handlers/flows/etalon.py` | Шаг 15 — мои эталоны (пустой список) |
| `msg_my_refs_list` | `handlers/flows/etalon.py` | Шаг 15 — мои эталоны (есть артикулы) |
| `msg_gen_photo_count` | `handlers/flows/gen_photo.py` | Шаг P1 — сколько фото |
| `msg_gen_photo_wish` | `handlers/flows/gen_photo.py` | Шаг P2 — пожелания |
| `msg_gen_photo_confirm` | `handlers/flows/gen_photo.py` | Шаг P3 — подтверждение |
| `msg_gen_photo_generating` | `handlers/flows/gen_photo.py` | Шаг P4 — создание |
| `msg_gen_video_count` | `handlers/flows/gen_video.py` | Шаг V1 — сколько видео |
| `msg_gen_video_wish` | `handlers/flows/gen_video.py` | Шаг V2 — пожелания |
| `msg_gen_video_confirm` | `handlers/flows/gen_video.py` | Шаг V3 — подтверждение |
| `msg_gen_video_generating` | `handlers/flows/gen_video.py` | Шаг V4 — создание |
| `msg_generation_done` | `handlers/flows/messages/common.py` | Результат создания фото |
| `msg_generation_done_failed_line` | `handlers/flows/messages/common.py` | Частичный fail для фото |
| `msg_generation_failed` | `handlers/flows/messages/common.py` | Ошибка создания фото |
| `msg_video_generation_done` | `handlers/flows/messages/common.py` | Результат создания видео |
| `msg_video_generation_done_failed_line` | `handlers/flows/messages/common.py` | Частичный fail для видео |
| `msg_video_generation_failed` | `handlers/flows/messages/common.py` | Ошибка создания видео |
| `msg_insufficient_funds` | `handlers/flows/messages/common.py` | Недостаточно средств |
| `msg_insufficient_funds_with_purpose` | `handlers/flows/messages/common.py` | Недостаточно средств (с purpose) |

---

### ✅ `services/pinterest_csv_generator.py` — выполнено

| list_key | Назначение |
|---|---|
| `pinterest_title_prefixes` | Случайные префиксы заголовков пинов (Новинка, Тренд, Хит…) |
| `pinterest_style_phrases` | Случайные стилевые фразы в описании пина |

### ✅ `services/image_watermark.py` — выполнено

| Ключ | Тип | Переменные |
|---|---|---|
| `watermark_article_label` | template | `{article}` |

Шаблон загружается в async-обёртке `apply_watermark_to_media_file`, результат передаётся как `article_label` в sync-функцию `apply_watermark`.

---

### ✅ `handlers/flows/messages/regen_reference.py` — выполнено

| Ключ | Функция | Переменные |
|---|---|---|
| `msg_ref_card` | `msg_ref_card()` | `{ref_number}`, `{total}`, `{article}`, `{category}` |
| `msg_regen_wish` | `msg_regen_wish()` | `{article}`, `{ref_number}` |
| `msg_regen_generating` | `msg_regen_generating()` | `{article}` |
| `msg_regen_result` | `msg_regen_result()` | `{article}`, `{ref_number}`, `{category}`, `{cost}`, `{balance}` |
| `msg_regen_no_source_photos` | `msg_regen_no_source_photos()` | `{article}` |

---

## Сводная таблица файлов

| Файл | Ответственность |
|---|---|
| `database/schema.sql` | Создание таблиц + seed-данные (idempotent) |
| `services/prompt_store.py` | TTL-кэш, `get_template()`, `get_list()`, `get_pairs()`, `get_banner()`, `invalidate()` |
| `services/prompt_generator_cloth.py` | Async-создатель фото-промптов, читает из store |
| `services/prompt_generator_video.py` | Async-создатель видео-промптов, читает из store |
| `services/pinterest_csv_generator.py` | Читает списки `pinterest_title_prefixes`, `pinterest_style_phrases` |
| `services/image_watermark.py` | Async-обёртка читает `watermark_article_label`, передаёт в sync `apply_watermark` |
| `handlers/flows/onboarding.py` | Шаги 1–2 — читает `msg_welcome`, `msg_profile` |
| `handlers/flows/new_article.py` | Шаги 3–5 — читает `msg_marketplace_select`, `msg_article_input`, `msg_product_found` |
| `handlers/flows/photo_selection.py` | Шаги 6–7 — читает `msg_photo_select`, `msg_reference_create_confirm` |
| `handlers/flows/create_reference.py` | Шаги 8–11 — читает `msg_reference_creating`, `msg_reference_generating_photo`, `msg_reference_ready` |
| `handlers/flows/etalon.py` | Шаг 15 — читает `msg_my_refs_empty`, `msg_my_refs_list` |
| `handlers/flows/gen_photo.py` | Шаги P1–P4 — читает 4 сообщения генерации фото |
| `handlers/flows/gen_video.py` | Шаги V1–V4 — читает 4 сообщения генерации видео |
| `handlers/flows/messages/common.py` | Результаты/ошибки генерации, нехватка средств — 8 сообщений |
| `handlers/flows/messages/regen_reference.py` | Шаги 16/16а — 5 сообщений, читает из store |

## Сводная таблица БД

| Таблица | Ключевые поля |
|---|---|
| `prompt_templates` | key (PK), template, description, banner, sort_order, updated_at |
| `prompt_list_items` | id, list_key, value, value2, is_active, sort_order, updated_at |
